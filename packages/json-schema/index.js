const JsonSchemaValidator = require('./lib').default;

Object.defineProperties(JsonSchemaValidator, {
    default: {
        value: JsonSchemaValidator,
        enumerable: false,
        writable: false,
        configurable: false,
    },
});

module.exports = JsonSchemaValidator;
